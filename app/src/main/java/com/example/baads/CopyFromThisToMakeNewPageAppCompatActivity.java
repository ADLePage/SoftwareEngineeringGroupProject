/*package com.example.baads;

import androidx.appcompat.app.AppCompatActivity;

import android.os.Bundle;

public class CopyFromThisToMakeNewPage extends AppCompatActivity {

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        //Change the part after R.layout. to get your page to load.
        setContentView(R.layout.activity_alarm_clock_new);

        //Change FragmentSecondBinding to what you changed above to.
        binding = FragmentAlarmclockBinding.inflate(getLayoutInflater());

        //Check my alarmclockNew for a howto of how to modify widgets on your page (functionwise)
    }
}*/